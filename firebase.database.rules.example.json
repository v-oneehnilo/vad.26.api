{
  "rules": {
    "shows": {
      "$showId": {
        ".read": true,
        ".write": true,
        "commands": {
          ".indexOn": ["timestamp"]
        },
        "events": {
          ".indexOn": ["timestamp"]
        }
      }
    }
  }
}
